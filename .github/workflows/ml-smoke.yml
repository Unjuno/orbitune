name: ml-smoke

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "orbitune/model.py"
      - "orbitune/lora.py"
      - "orbitune/training.py"
      - "orbitune/inference.py"
      - "orbitune/tokenizer/**"
      - "orbitune/release.py"
      - "scripts/smoke_train.py"
      - "scripts/package_base_release.py"
      - "pyproject.toml"
      - ".github/workflows/ml-smoke.yml"
  pull_request:
    paths:
      - "orbitune/model.py"
      - "orbitune/lora.py"
      - "orbitune/training.py"
      - "orbitune/inference.py"
      - "orbitune/tokenizer/**"
      - "orbitune/release.py"
      - "scripts/smoke_train.py"
      - "scripts/package_base_release.py"
      - "pyproject.toml"
      - ".github/workflows/ml-smoke.yml"

jobs:
  train-smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install CPU PyTorch
        run: python -m pip install torch==2.10.0 --index-url https://download.pytorch.org/whl/cpu
      - name: Install ML dependencies
        run: python -m pip install -e .[dev]
      - name: Train full fixed Base and rank-4 LoRA
        run: |
          python scripts/smoke_train.py \
            --base-steps 5 \
            --adapter-steps 5 \
            --device cpu \
            --out smoke-training-report.json
      - name: Assert measured smoke report contract
        run: |
          python - <<'PY'
          import json
          report = json.load(open('smoke-training-report.json', encoding='utf-8'))
          assert report['base']['parameters'] == 2_945_760
          assert report['adapter']['trainable_parameters'] == 15_360
          assert report['base']['validation_loss'] > 0
          assert report['adapter']['validation_loss'] > 0
          print('ML smoke contract PASS')
          PY
      - name: Upload report
        uses: actions/upload-artifact@v4
        with:
          name: ml-smoke-report
          path: smoke-training-report.json
